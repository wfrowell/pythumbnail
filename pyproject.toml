[project]
name = "pythumbnail"
authors = [{name = "wfrowell", email = ""}]
version = "2.0.1"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "ossapi",
    "rosu_pp_py",
    "pillow",
    "osrparse"
]

[project.optional-dependencies]
dev = [
    "pytest"
]
